# LearnAble Full Ready App

Run:
1. Optional: start Ollama locally and pull a model, for example:
   ollama pull phi:latest
2. Install:
   npm install
3. Start:
   npm run dev

This app supports both:
- AI mode when Ollama is running
- Demo mode fallback when Ollama is offline
